package structual_pattern.decorator;
/**
 * Component: interface chung để các đối tượng cần thêm chức năng trong quá trình chạy(runtime)
 * thì triển khai interface này.*/
public interface IPizza {
    String doPizza();
}
